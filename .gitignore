/target
dist
# The web client's production build (vite-plugin-singlefile) emits
# crates/pi-cli/web/dist/index.html; it is generated output, never tracked.
node_modules
crates/pi-cli/web/.web-test-build/
crates/pi-cli/web/.transcript-test-build/
crates/pi-cli/web/.panel-shots/
/examples/target
*.log
/.pi/artifacts/
/diagram-output/
session.html
/.pi-subagents/
# Secrets and local credentials — never commit.
.env
.env.*
!.env.example
*.pem
*.key
auth.json
credentials*
# Test residue: integration tests run with the crate root as cwd and a
# relative session-dir, writing native sessions here.
/crates/pi-cli/sessions/
__pycache__
book
.web-test-build/
